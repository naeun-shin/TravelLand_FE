import Button, { LargeButton } from '@/components/commons/buttons/Button';
const Main = () => {
  return (
    <>
      <Button text="가족여행" />
      <LargeButton text="큰 버튼" />
    </>
  );
};

export default Main;
